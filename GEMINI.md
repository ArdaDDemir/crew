# GEMINI.md

Follow **[AGENTS.md](./AGENTS.md)** before writing code.

Not a Discord bot. Not a wrapper around Gemini CLI. Local bots + channels + `@` mentions + DMs. v1 is a Bun CLI. `packages/core` has no I/O to the network. Tests: `bun test`. TDD. ADRs in `docs/adr/`.
