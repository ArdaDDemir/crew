# GEMINI.md

Follow **[AGENTS.md](./AGENTS.md)** before writing code.

Not a Discord API bot. Not a wrapper around Gemini CLI. Local bots + channels + `@` mentions + DMs. Surface: `bun run ui` or Crew.exe (0.9.0). CLI is tests/scripts. `packages/core` has no network I/O. Tests: `bun test`. TDD. ADRs in `docs/adr/`.
