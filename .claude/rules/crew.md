# Claude Code project rules

Follow `/AGENTS.md`. This is a local multi-bot runtime (`bun run ui`; CLI is tests/scripts), not Discord and not a harness wrapper.

TDD, ADRs, `packages/core` without I/O, mention-based wake routing.
